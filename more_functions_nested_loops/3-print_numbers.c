#include "main.h"

/**
 * print_numbers - rints the numbers, from 0 to 9
 *
 * Return: Always 0 (Sucess)
 */
void print_numbers(void)
{
	int i = 48;

	for (i = 48 ; i <= 57 ; i++)
	{
		_putchar(i);
	}
	_putchar(10);
}
